name: CI

on: [ push ]

env:
  TERM: dumb
jobs:

  integration-test-for-cosmos-1:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 8
        uses: actions/setup-java@v3
        with:
          java-version: '8'
          distribution: 'temurin'

      - name: Generate unique prefix using the epoch
        run: |
          echo "db_prefix=$(date +%s%3N)" >> $GITHUB_ENV

      - name: Setup and execute Gradle 'integrationTestCosmos' task
        uses: gradle/gradle-build-action@v2
        with:
          arguments: integrationTestCosmos --debug -Dscalardb.cosmos.uri=${{ secrets.COSMOS_URI }} -Dscalardb.cosmos.password=${{ secrets.COSMOS_PASSWORD }} -Dscalardb.cosmos.database_prefix=${{ env.db_prefix }}

      - name: Save Gradle test reports
        if: always()
        run: |
          mkdir -p /tmp/gradle_integration_test_reports/core
          mkdir -p /tmp/gradle_integration_test_reports/schema-loader
          cp -a core/build/reports/tests/integrationTestCosmos /tmp/gradle_integration_test_reports/core/
          cp -a schema-loader/build/reports/tests/integrationTestCosmos /tmp/gradle_integration_test_reports/schema-loader/

      - name: Upload Gradle test reports
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: gradle_cosmos_integration_1_test_reports
          path: /tmp/gradle_integration_test_reports

  integration-test-for-cosmos-2:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 8
        uses: actions/setup-java@v3
        with:
          java-version: '8'
          distribution: 'temurin'

      - name: Generate unique prefix using the epoch
        run: |
          echo "db_prefix=$(date +%s%3N)" >> $GITHUB_ENV

      - name: Setup and execute Gradle 'integrationTestCosmos' task
        uses: gradle/gradle-build-action@v2
        with:
          arguments: integrationTestCosmos --debug -Dscalardb.cosmos.uri=${{ secrets.COSMOS_URI }} -Dscalardb.cosmos.password=${{ secrets.COSMOS_PASSWORD }} -Dscalardb.cosmos.database_prefix=${{ env.db_prefix }}

      - name: Save Gradle test reports
        if: always()
        run: |
          mkdir -p /tmp/gradle_integration_test_reports/core
          mkdir -p /tmp/gradle_integration_test_reports/schema-loader
          cp -a core/build/reports/tests/integrationTestCosmos /tmp/gradle_integration_test_reports/core/
          cp -a schema-loader/build/reports/tests/integrationTestCosmos /tmp/gradle_integration_test_reports/schema-loader/

      - name: Upload Gradle test reports
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: gradle_cosmos_integration_2_test_reports
          path: /tmp/gradle_integration_test_reports

  integration-test-for-cosmos-3:
    runs-on: self-hosted

    steps:
      - uses: actions/checkout@v3

      - name: Set up JDK 8
        uses: actions/setup-java@v3
        with:
          java-version: '8'
          distribution: 'temurin'

      - name: Generate unique prefix using the epoch
        run: |
          echo "db_prefix=$(date +%s%3N)" >> $GITHUB_ENV

      - name: Setup and execute Gradle 'integrationTestCosmos' task
        uses: gradle/gradle-build-action@v2
        with:
          arguments: integrationTestCosmos --debug -Dscalardb.cosmos.uri=${{ secrets.COSMOS_URI }} -Dscalardb.cosmos.password=${{ secrets.COSMOS_PASSWORD }} -Dscalardb.cosmos.database_prefix=${{ env.db_prefix }}

      - name: Save Gradle test reports
        if: always()
        run: |
          mkdir -p /tmp/gradle_integration_test_reports/core
          mkdir -p /tmp/gradle_integration_test_reports/schema-loader
          cp -a core/build/reports/tests/integrationTestCosmos /tmp/gradle_integration_test_reports/core/
          cp -a schema-loader/build/reports/tests/integrationTestCosmos /tmp/gradle_integration_test_reports/schema-loader/

      - name: Upload Gradle test reports
        if: always()
        uses: actions/upload-artifact@v3
        with:
          name: gradle_cosmos_integration_3_test_reports
          path: /tmp/gradle_integration_test_reports

