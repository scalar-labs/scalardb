package com.scalar.db.transaction.consensuscommit.replication.semisync.columns;

public class IntColumn extends Column<Long> {
  public IntColumn(String name, Long value) {
    super(name, value);
  }
}
