# Expanded at build time from the Gradle icu4jVersion property; consumed by the
# CollationComparator runtime check that warns when the ICU4J library loaded at runtime differs
# from the version this ScalarDB build was verified against.
icu4j.version=${icu4jVersion}
